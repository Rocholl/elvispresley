# URL del repo privado (SSH con deploy key, recomendado)
GIT_REPO_URL=git@github.com:Rocholl/elvispresley.git

# Alternativa HTTPS con token de GitHub (fine-grained, solo lectura)
# GIT_REPO_URL=https://x-access-token:ghp_XXXX@github.com/TU_USUARIO/abuela.git

SMULE_USER=ElvaTorales1

# Ruta local a la deploy key (solo lectura, montada en el contenedor)
DEPLOY_KEY_PATH=./deploy-key
